export const heroBanner = `*[_type == "banner" && status == "public"]{
    _id,
    _type,
    longText,
    title,
    image,
    primaryButton,
    secondaryButton,
    shortText,
    longText,
}`;
